packages: dnsbase.cabal

package dnsbase
    tests: True
    benchmarks: False

constraints:
  base installed,
  ghc installed,
  ghc-bignum installed,
  ghc-prim installed,
  integer-gmp installed,
  template-haskell installed
